import express from "express";
import * as userController from '../controllers/usersControllers'
const router = express.Router();

router
    // .get("/get-user", userController.getUser)
    // .get("/get-users", userController.getAllUsers)
    // .post("/add-user", userController.addUser);

