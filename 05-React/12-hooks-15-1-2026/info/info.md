# Hooks

useXYZ is a naming convention for custom React hooks. Custom hooks are JavaScript functions whose names start with "use" and that may call other hooks. This convention is important because it allows React to automatically check for violations of the rules of hooks.