let x = 18;

// for (let i = 2; i < x; i++) {

//   if (x % i === 0) {
//     console.log(`${x} is not a prime number because it is divisible by ${i}.`);
//   } else {
//     console.log(`${x} is a prime number because it is not divisible by ${i}.`);
//   }

// }

// if (x % 2 !== 0) {
//   console.log(`${x} is a prime number.`);
// }

if (x % 2 !== 0) {
  console.log(`${x} is a prime number.`);
} else {
  console.log(`${x} is not a prime.`);
}