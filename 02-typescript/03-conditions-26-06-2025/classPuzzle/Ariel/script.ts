let udiAge: number = 78;
let shaniAge: number = 25;
let oldAge = 75;

if (udiAge >= oldAge) {
  console.log(`you can move to the front of the queue!`);
} else console.log(`you have to wait in line!`);

if (shaniAge >= oldAge) {
  console.log(`you can move to the front of the queue!`);
} else console.log(`you have to wait in line!`);

// let namePrompt = prompt(`Enter your name:`);
// let agePrompt = Number(prompt(`Enter your age:`));

// if (agePrompt >= oldAge) {
//   console.log(`Hello! ${namePrompt}, you can move to the front of the queue!`);
// } else console.log(`you have to wait in line!`);