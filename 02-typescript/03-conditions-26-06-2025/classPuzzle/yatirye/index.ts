let pass:string = " can move to the front of the queue";
let noPass:string = " have to wait in line";
let age:number =75;
let udi:number = 78;
let shani:number = 25;
let maya:number = 76;
let adam:number = 73;
let dror:number = 18;


if (udi >= age) {
    console.log( "udi" + pass);
} else{console.warn("udi" + noPass);}

if (shani >= age) {
    console.log( "shani" + pass);
} else{console.warn("shani" + noPass);}

if (maya >= age) {
    console.log( "maya" + pass);
} else{console.warn("maya" + noPass);}

if (adam >= age) {
    console.log( "adam" + pass);
} else{console.warn("adam" + noPass);}

if (dror >= age) {
    console.log( "dror" + pass);
} else{console.warn("dror" + noPass);}